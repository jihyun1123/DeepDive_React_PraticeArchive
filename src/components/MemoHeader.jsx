import './MemoHeader.css';

export default function MemoHeader(){
    return(
        <header className="memo-header">
            <h1>📝 메모 관리 앱</h1>
            <p>React + axios + CRUD 프로젝트</p>
        </header>
    );
}